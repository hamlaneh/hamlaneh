# Design Status Registry

Source of truth for which screens have a finished mockup. Frontend agents check this table
before building any screen (see CLAUDE.md "UI pipeline"). The user updates rows when Claude
Design deliverables land. `PENDING` → build unstyled functional UI only.

Functional requirements per screen (for producing the designs): [BRIEFS.md](BRIEFS.md).

| Screen set | Brief | Status | Mockup |
|---|---|---|---|
| Login & password reset (10 artboards) | BRIEFS.md §1 | **DESIGNED** — delivered 2026-08-21 | `Hamlaneh Auth.dc.html` ([canvas](https://claude.ai/design/p/185f4552-36fc-489e-a9b1-c012ab74f6d6), handoff copied to [LOGIN_HANDOFF.md](LOGIN_HANDOFF.md)) |
| Chat shell (9 artboards + components) | BRIEFS.md §2 | **DESIGNED** — delivered 2026-08-21 | `Hamlaneh Chat.dc.html` (handoff: [CHAT_HANDOFF.md](CHAT_HANDOFF.md)) |
| Admin dashboard (8 artboards + components) | BRIEFS.md §3 | **DESIGNED** — delivered 2026-08-21 | `Hamlaneh Admin.dc.html` (handoff: [ADMIN_HANDOFF.md](ADMIN_HANDOFF.md)) |
| User settings (6 artboards + components) | BRIEFS.md §4 | **DESIGNED** — delivered 2026-08-21 | `Hamlaneh Settings.dc.html` (handoff: [SETTINGS_HANDOFF.md](SETTINGS_HANDOFF.md)) |
| Call / meeting view | BRIEFS.md §5 | **DELIVERED 2026-08-29** — [`mockups/Hamlaneh Chat Calls.dc.html`](mockups/Hamlaneh%20Chat%20Calls.dc.html), handoff [`CALLS_HANDOFF.md`](CALLS_HANDOFF.md). Nine artboards: `call-prejoin`, `call-grid`, `call-screenshare`, `call-banner`, `call-ring`, `meet-guest`, `meet-guest-dead`, `call-rtl-fa`, `call-states`. Every question this row asked is answered there: the tile borrows the sidebar's avatar treatment and restates the frame; the call is a sibling of the message column and collapses to the banner with **Return to call** when you navigate away, which is what makes it leavable; the banner is one strip with two states in a permanent slot, so going live never shoves the message list; Leave is a danger fill at the logical inline-end behind a gap and a rule, with the toggles clustered at the opposite end. No new token, font or typography step. **`implemented` 2026-08-29** — `webapp/src/components/calls/` carries the design: the camera-off tile as the designed case, per-tile state as a word and a shape as well as a colour, the 2/3/5/12 grid ladder with a presentational count cell beyond twelve, the share large with a 212 rail, the one-strip two-state banner in its permanent 60px slot, and Leave at the logical inline-end behind a gap and its own rule. Three artboard states are drawn but not built, because each needs data or behaviour the plumbing does not have: `call-prejoin` (there is no pre-join screen — a join publishes the microphone with the camera off), `tile-connection-poor` and `tile-reconnecting` (`MediaParticipant` carries no quality or reconnecting flag), and the `Rejoin` action and `room-waiting-alone` on `call-states`; the `+N more` cell names the people it stands for but cannot order them by most recent speaker, because `MediaParticipant` carries no last-spoke time either. **Where the call lives, settled 2026-08-29:** it mounts in the upper region of the channel pane at the artboard's own 496px, a sibling of the message column with the conversation and the composer continuing beneath it, and navigating to another channel no longer strands anybody — the call collapses to the banner strip's third state (`calls.strip.away` + **Return to call**), stays connected, and the way back is the route to Leave. **The screen-share leak warning shipped with it**: a persistent `role="status"` band between the header and the stage while sharing, carrying "You are sharing your whole screen", what that exposes, and its own 44px **Stop sharing** — deliberately duplicating the bar's share toggle, since somebody who has forgotten they are sharing is not looking at the control bar. | ✅ |
| Guest meeting page (`meet-guest`) | BRIEFS.md §5 | **DELIVERED 2026-08-29** — `meet-guest` and `meet-guest-dead` on [`mockups/Hamlaneh Chat Calls.dc.html`](mockups/Hamlaneh%20Chat%20Calls.dc.html). The four questions are answered: the instance appears as one line of text and no logo lockup, because a wordmark over a centred card *is* the front-door treatment; the language switcher sits in the page's own top rule at the logical inline-end, identity at one end and language at the other; the name field and Join stack at 375 and share a row at ≥1280; and the in-call surface is the entire page, same tiles and control bar with the sidebar and message column simply absent. It refuses `AuthShell`, `AuthForm`, `PrimaryButton`, the centred card on a tinted ground, the wordmark lockup, and any link to sign-in — and the name field is `autocomplete="name"`, never `username` or `email`, since a password manager offering to save credentials would say the opposite of the copy. **Reskin shipped 2026-08-29** in `webapp/src/screens/meet.css` + `MeetGuestScreen.tsx`: the page wears its own rule, flush single column, 52px field and Join, neutral guest note and the dead-link glyph — none of `AuthShell`, `AuthForm`, `PrimaryButton` or `.hm-button`, and no link anywhere except the front door. One departure remains, reported rather than invented: the drawn trailing `arrow-right` is the delivered `ChevronRightIcon` under `hm-mirror-glyph`, since adding an icon was out of the reskin's scope. **The identity line landed 2026-08-29**, once the contract change the reskin asked for arrived — `ConferencePreview.org_name`, the same thing `InvitePreview` already names on an equally unauthenticated screen. The rule now reads "«Org» is hosting this meeting" as the artboard draws it: the organisation's name and the clause as two typographic pieces on one line, no logo lockup, and never the product's own name, which would claim Hamlaneh is the host and be the refused wordmark by another route. A dead link still carries no name, because a 404 carries no body — the page knows the link failed and nothing about who issued it, and guessing is what the rest of this screen refuses to do. | ✅ |
| Meeting links (Settings → Meetings) | — | **DELIVERED 2026-08-29** — `settings-meetings` appended to [`mockups/Hamlaneh Settings.dc.html`](mockups/Hamlaneh%20Settings.dc.html). A records table **is** right, once the absence is stated rather than left as a gap: no blank link cell, no disabled Copy, no tooltip — a note above the table, in the reader's path. "Someone is in it" is a filled dot with a ring plus the words, and the header says "Read when this panel opened" with a Refresh, because nothing feeds this screen live and a pulse would promise liveness it does not have. Creation is the header action opening a dialog, as `Create invite link` is, since it ends in the show-once panel and the act is already modal. It stays in Settings. **Reskin shipped 2026-08-29** in `webapp/src/components/settings/MeetingsSection.tsx` + `settings.css`: the delivered admin table treatment, the destructive row outline, the admin create-dialog for the header action, the neutral note banner above the table, and a presence dot that never animates. The three missing keys landed as `settings.meetings.noLinkColumn`, `readWhenOpened` and `refresh`; `admin.error.retry` ("Try again") was kept for the load-failure retry and **not** reused for Refresh, which is a deliberate re-read of a snapshot rather than a recovery from an error. Two further keys the layout change forced: `createSubmit` (the dialog's own action, mirroring `admin.invites.createSubmit`) and `yourLinks` (the table-group label the artboard's header strip displays). | ✅ |
| Chat overlays — create channel, people picker (invite + new DM), mention picker | BRIEFS.md §2 addendum | **DESIGNED and IMPLEMENTED** — delivered 2026-08-21 as 13 `chat-addendum-*` artboards, reskinned 2026-09-02. The `@`-query mention behaviour the addendum requires as a product change, not a reskin, shipped with it: the list opens from an `@` at the composer token as well as from the Mention control, one combobox over one listbox, focus never leaving the composer. **Four deltas, each because an existing test pins the older contract and a test is not weakened to match a picture:** the person-row accessible name stays `{action}: {display name}` rather than the sheet's `Invite X, @user, to #slug` (also needs a channel prop the callers do not pass); a duplicate slug stays a banner rather than the field-level error (`chat.createChannel.duplicate` exists, unwired); the account trigger stays "Account" rather than "Open account menu"; the two dialog accessible names stay their bare titles. Also unimplemented: progressive cursor loading in the picker (§03) — no control is drawn for it and the current call is a flat limit | `Hamlaneh Chat.dc.html` (handoff: [CHAT_HANDOFF.md](CHAT_HANDOFF.md)) |
| Channel menu, account menu | BRIEFS.md §2 addendum 2 | **DESIGNED and IMPLEMENTED** — delivered 2026-08-21 as 11 further `chat-addendum-*` artboards, reskinned 2026-09-02 as anchored non-modal dialogs (`role="dialog"`, `aria-modal="false"`), not ARIA menus. The locked correction propagated: the channel-actions trigger is 44×44 everywhere and now renders on mobile. **Two design questions the sheet raises and the shell cannot yet answer — yours to decide:** (1) the sheet says Channel actions is absent in a DM, but the DM branch is the only calm entry to the E2EE verification sheet, so it renders there only when verification is available; (2) the sheet says Admin "stays separate", but no separate Admin control exists, so removing the row would remove the only route to `/admin` — it is kept, styled as a menu row | `Hamlaneh Chat.dc.html` (handoff: [CHAT_HANDOFF.md](CHAT_HANDOFF.md)) |
| Encrypted-call indicator | BRIEFS.md §Media E2EE | `awaiting-design` — unstyled plumbing built in slice 3.4 (`components/calls/CallView.tsx`, `calls.encrypted.*`): three plain sentences above the stage, drawn only when the call is actually keyed. Says a DM or channel call is end-to-end encrypted, and must not imply metadata protection the SFU does not give (it still sees who is talking to whom, when, and who is speaking). **The claim and the disclaimer are one statement and the artboard may not keep the first without the second** — a true sentence about what the server cannot read, standing alone, is read by most people as a claim about who it cannot see. **The artboard must answer:** how a true claim about content sits next to an absent claim about metadata without either being read as the other | — |
| Conference plain-labelling | BRIEFS.md §Media E2EE | `awaiting-design` — unstyled plumbing built in slice 3.4 (`screens/MeetGuestScreen.tsx`, `meet.mediaLabel` + `meet.mediaWhy`): two sentences under the guest note on the join form. A conference is NOT end-to-end encrypted in either mode, and the join surface says the server that hosts it can access its audio and video. The unqualified word "encrypted" may not appear on it — the shipped copy says "not end-to-end encrypted" and "protected in transit", never the bare word. The second sentence exists so a working, deliberate feature does not read as a broken one. **The artboard must answer:** how to state that plainly without making a working, legitimate feature look broken | — |
| Mid-call publish warning | BRIEFS.md §Media E2EE | `awaiting-design` — unstyled plumbing built in slice 3.4: it is literally slice 3.3's `VerificationWarning`, rendered above the call stage with two sentences swapped (`calls.blocked.title` names the stopped microphone and camera and says it is not a hardware fault; `calls.blocked.continues` says hearing carries on). The same records, the same two exits, and still no third. The three publish toggles are disabled while it stands, and the session refuses them as well, so the gate is not the buttons. **The artboard must answer:** what a call looks like when your own camera and microphone stop for a reason that is not an error | — |
| Key verification sheet (`verification-sheet`) | BRIEFS.md §Key verification | `awaiting-design` — unstyled plumbing built in slice 3.3 (`plumbing/Verification.tsx`): the safety number as 60 ASCII digits in twelve groups, direction-pinned LTR, and the three states as three distinct strings. **No QR** — deferred, the digits are what the ceremony needs and ADR 008 leaves the encoding of a QR open. **The artboard must answer:** how far apart pinned and verified look, since rendering them alike would make an unceremonied acceptance read as a proof | — |
| Changed-key warning (`verification-changed`) | BRIEFS.md §Key verification | `awaiting-design` — unstyled plumbing built in slice 3.3; replaces the composer rather than disabling it, names who and what changed (new device vs replaced key), and renders exactly two buttons with no dismiss. **The artboard must answer:** how a warning that blocks sending while reading continues sits in the message column without reading as an error page | — |
| Own-account device prompt | BRIEFS.md §Key verification | `awaiting-design` — unstyled plumbing built in slice 3.3 as the first block inside the changed-key warning, so it is the reader's own account they meet first. **The artboard must answer:** what makes this louder than the peer warning without becoming a modal that a user learns to dismiss | — |
| Recovery-code entry on `login-totp` | BRIEFS.md §1 | `awaiting-design` — the `login-totp` artboard draws only the six authenticator cells, so a user who lost their authenticator had nowhere to type the recovery codes enrolment gave them; the screen now switches between the six cells and one plain recovery-code field, built entirely from delivered parts (`TextField`, `hm-text-button`) with no new visual treatment | — (needs a `login-totp-recovery` variant of `Hamlaneh Auth.dc.html`) |
| Empty direct message | BRIEFS.md §3 | `awaiting-design` — the `chat-empty-channel` artboard is a channel: it names the conversation by slug, offers "Invite people" and "Set a topic", and closes with "Only you can see this channel until someone is invited". A DM has no slug, refuses both actions with 400, and already has its other person in it, so all three were false. Built from the same shell with the peer's avatar in the glyph slot, the peer's name in the title, and the two actions and the note simply not rendered | — (needs a `chat-empty-dm` variant of `Hamlaneh Chat.dc.html`) |
| Composer attachment tray | BRIEFS.md §2 | **decided by the orchestrator, 2026-08-26** (indeterminate "Uploading…", a refusal persists until removed, tray above the composer box — a determinate percentage would mean rewriting the upload as XHR, since fetch reports no upload progress, to buy a number that is wrong on a fast link anyway) — the artboards draw the paperclip and the finished file/image cards, but nothing draws a file *between* pick and send. Built from delivered parts only: a finished upload renders its own delivered card, and the two states with no card of their own (uploading, and a refusal) reuse the `hm-card` shell with the status on its meta line, each with an `X` remove control. **Questions for the designer:** (1) is per-file progress a determinate bar, or is "Uploading…" enough — `fetch` reports no upload progress, so a percentage would need the request rewritten as XHR; (2) does a failed upload stay in the tray until removed (current behaviour, and what keeps send from silently dropping it) or vanish with a transient message; (3) where does the tray sit relative to the composer box and the markdown hint row | — (needs composer states on `Hamlaneh Chat.dc.html`) |
| Encrypted file card and its cannot-open state | BRIEFS.md §2 | `awaiting-design` — no artboard draws a file in an encrypted conversation, and it is not the plaintext card with different data: the server's row for one carries the literal filename `encrypted`, the opaque type and the ciphertext size, so every word on the card comes from the decrypted message instead, and the bytes arrive only after a fetch and a decrypt. Built in the encrypted-attachments slice from delivered parts only (`components/chat/EncryptedAttachments.tsx`), on the precedent this table set for the composer tray: the delivered `hm-card` and `hm-image-card` shells, with the three states that have no card of their own — opening, failed, and cannot be opened at all — written on the meta line. **One departure from the plaintext card is structural rather than visual and must survive any reskin:** the save control is a `<button>` and the card holds no `href` anywhere, because a decrypted `blob:` URL inherits the app origin and navigating to one would hand a sender that origin (ADR 013). An artboard that draws the download as a link would be asking for the one thing this card may not do. **The artboard has to answer:** whether an encrypted file should look identical to a plaintext one — it is the same object to the reader, and a difference would either reassure or alarm without either being true; what a file that will never open looks like beside messages that did (a member who joined after it was sent cannot open it, ever, and the copy says so); and whether the moment between pressing save and the file arriving deserves anything more than a word on the meta line, given that the whole file has to be fetched and authenticated before one byte of it can be shown | — (needs an encrypted-conversation file state on `Hamlaneh Chat.dc.html`) |
| Expired attachment URL | BRIEFS.md §2 | **decided by the orchestrator, 2026-08-26**: the fix is upstream and draws no new state — a channel whose links have aged out re-fetches its history through the loading state already drawn, minting fresh URLs; an expired click survives only in a tab left open past that, and falls back to the drawn no-thumbnail glyph — `Attachment.url`/`thumbnail_url` are signed for about an hour and the contract says never to store them, but the chat store keeps message objects for the life of the tab and re-entering a loaded channel issues no fetch, so a tab open longer than an hour holds dead links. An `<img>` that already painted stays painted; one that has not yet loaded now falls back to the card's **delivered** no-thumbnail glyph (`AttachmentCards`, `onError`). A **download click after expiry gets the files origin's bare 404** — no drawn state covers it, and nothing client-side can distinguish it from a deleted file. **Question for the designer:** what a reader should see when a download refuses, or whether the fix belongs upstream (the orchestrator's call: re-fetch the channel's history when its links age out, which would reuse the drawn loading state and need no new artboard) | — |
| Sign-in and reset screens at phone width | BRIEFS.md §1 | `awaiting-design` — the auth set is drawn at 1440 and 900, and its own layout is already mobile-first below 900, so almost all of it survives 375 as delivered. One thing does not: the six drawn one-time-code cells are 60px wide and come to 400px with their gaps, against 375px of phone. The cells are now allowed to shrink to fit and keep the drawn 60px at every width with room for it — a deliberate stretch of a delivered value, not a new one. **The artboard has to answer:** what the code row is below 400px — six narrower cells, a matching drop in cell height (60px tall and 47px wide is a different proportion from the one drawn), or one plain field; and whether the 60px cell is a minimum or a target | — (needs a 375 variant of `login-totp` on `Hamlaneh Auth.dc.html`) |
| Settings panel at phone width | BRIEFS.md §4 | `awaiting-design` — the panel is drawn at 1040×720 and there is no phone artboard. It survives 375 on delivered parts alone: the panel is `min(1040px, 100%)`, its columns already wrap, and below 899 the navigation rail becomes a horizontal strip above the content. **The artboard has to answer:** whether a phone gets that strip or a full-screen panel with a back affordance per section; whether the panel keeps its scrim, radius and inset at a width where they cost most of the screen; and what the recovery-code list and the QR step look like when neither has room for its drawn column | — (needs a 375 variant of `Hamlaneh Settings.dc.html`) |
| Admin dashboard at phone width | BRIEFS.md §3 addendum 3e | `awaiting-design` — drawn at 1440 with a 260px rail and a 40px gutter, which leaves ~75px of table on a 375px phone, and it had no responsive rules at all. Below 899 it now stacks: the rail becomes a horizontal strip above the content (the same pattern the settings panel already uses at this breakpoint), the gutter drops to the 16px token, the two panes stop scrolling independently and the document scrolls as one. DOM order is untouched, so the tab order is unchanged. The tables keep the `overflow-x: auto` card they already had. **The artboard has to answer:** whether a records table is a table at all on a phone or a stack of rows; where the org identity, the exit and the signed-in-as footer go when three stacked blocks of chrome sit above the first row; and whether the row menu, which is 260px and anchored to the row's end edge, becomes a sheet | — (needs a 375 variant of `Hamlaneh Admin.dc.html`) |
| Persian numerals (every screen) | — | **Resolved 2026-08-26 — follows the handoff** | The locked correction of 2026-08-21 ([CHAT_HANDOFF.md](CHAT_HANDOFF.md), "Numerals") is the answer: ASCII `0-9` for every app-generated number in the Persian UI, and it says in as many words that it supersedes the artboards disagreeing with it. Applied in 1.5 across all three places it had to hold at once — i18next's `number` formatter, chat's own formatters, and the e2e helper that predicts what the screen will say — from one rule in `webapp/src/i18n/digits.ts`; the 11 hand-authored Persian numerals in `fa/common.json` were converted with them. The two `src/App.test.tsx` tests now assert Latin digits and still check that `Intl.NumberFormat("fa")` alone yields Persian ones, so dropping the pin fails loudly instead of quietly. |
| Forced two-step enrolment | BRIEFS.md §1 | `awaiting-design` — no artboard draws enrolment a person did not choose. `login-force-password-change` is the only forced-state artboard and it is a narrow auth column around one form; the enrolment flow is the three-step `settings-2fa-setup` sequence, drawn for a 1040px settings panel with a QR beside a column of instructions, and nothing draws the two together. Built as plain semantic HTML (a heading and a paragraph saying whose requirement this is) wrapping the delivered `TwoFactorSetup` unchanged, with every exit relabelled "Sign out instead" because that is the only way out. **The artboard has to answer:** whether this is the auth column or the settings panel width when there is no panel and no rail to sit in; where the "your organization requires this" sentence goes relative to the step header, which already occupies the top row with a back control; and what the back control is at all when there is no section to go back to — sign-out, or nothing | — (needs a `login-force-totp-enrolment` set on `Hamlaneh Auth.dc.html`, or the 2FA setup steps redrawn outside the settings panel) |
| Single sign-on button on sign-in | BRIEFS.md §1 | `awaiting-design` — the auth set draws exactly one way into the instance: the identifier/password form with its single full-width CTA. Single sign-on is a second door beside it, and no artboard draws a second door, a divider between two ways in, or a provider-branded control. Built as plain semantic HTML — an `<a>` to `/api/v1/auth/oidc/start` below the CTA, no class, no delivered treatment borrowed — because borrowing `PrimaryButton` would state "this is the primary action" and borrowing `hm-link` would state "this is an afterthought like Forgot password?", and which of those it is, is the designer's call. It is a link rather than a button because the flow is a browser navigation. **The artboard has to answer:** whether the provider is the primary way in or the secondary one on an SSO instance (which reorders the whole column); whether the two are separated by an "or" divider, and what it says; whether the control carries the provider's name only, or a mark as well (a mark means an asset per provider and a fallback for providers with none); and how the button behaves at 375, where "Continue with «a long provider name»" is the widest string on the screen | — (needs an `login-sso` variant of `Hamlaneh Auth.dc.html`) |
| Single sign-on linking (Settings → Security) | BRIEFS.md §4 | `awaiting-design` — nothing in the settings set draws account linking; `settings-security` has the password form, the two-step card and the sessions summary, and stops there. Built as plain semantic HTML — a bare `<section>` with a heading, one sentence, a `<button>` and a `role="alert"` paragraph — deliberately *not* reusing `hm-settings-card`, `SettingsButton` or the on/off state pill, since which of those treatments this deserves is exactly what is undrawn. Disconnect fires immediately with no confirm step: the delivered `ConfirmDialog` re-asks for the password, and the contract does not, so using it would invent a requirement. **The artboard has to answer:** whether this is a card of its own or a row inside the two-step card; whether connected/not-connected uses the same state pill two-step uses, and what the pill says for a state that is not on/off; whether disconnecting — which for a password account is a real loss of a sign-in method — needs a confirm step of its own, and whether the "your account has no password, ask an administrator" refusal is an inline notice, a disabled control with a reason, or a dialog | — (needs a single sign-on block on `Hamlaneh Settings.dc.html`) |
| Conversation unavailable | BRIEFS.md §3 | `awaiting-design` — no artboard covers landing on `/c/{id}` for a channel you are not in, which is what a stale permalink or a revoked invitation produces. It rendered the empty-account invitation beside a sidebar listing the reader's actual conversations. Uses the same `hm-empty__body` paragraph the loading and failed-load states use, with a sentence that points at the list and commits to nothing about whether the id names anything | — (needs a state on the chat artboard) |
| SCIM provisioning tokens (Admin → Provisioning) | BRIEFS.md §3 addendum 3c | `awaiting-design` — the admin set draws four sections and this is a fifth, arriving after the artboards. Its body is plain semantic HTML with no delivered treatment borrowed: a paragraph, an inline `<form>` with one text field, a bare `<table>`, and plain `<button>`s. Two things are reused rather than rebuilt, because each is the same act already drawn: the show-once panel (`admin-create-user-credentials`) for the minted token — a value the server keeps only a hash of, exactly like an invite link — and the confirm dialog for revoking, which invite revocation already uses. The nav row was **appended after "Audit log"** rather than inserted, so the drawn four keep their drawn positions, and it borrows `ShieldIcon` from the settings Security rail because no glyph is drawn for it. **The artboard has to answer:** whether a records table is the right form when the most useful column (`Last used`) is empty on every token nobody has configured yet; how "never used" is drawn — plain text as built, the Active/Deactivated state pill the users table uses, or a warning tone, given that it is unremarkable an hour after minting and a real signal a month after; whether minting lives in the header `action` slot the users and invites sections use (which makes it a dialog, like "Create invite link") or stays an inline form on the page; and what glyph the fifth nav row carries | — (needs an `admin-scim` artboard on `Hamlaneh Admin.dc.html`) |
| Just-in-time provisioning toggle (Admin → Org settings) | BRIEFS.md §3 addendum 3d | `awaiting-design` — built from delivered parts only, so no new treatment was invented: the `hm-admin-switchrow` + `hm-admin-toggle` pair the artboard already draws for "Require two-step verification", with the label/hint text block that control already carries. What the artboard does not draw is that pair **in the account-creation panel**, which it draws as a radio group and one warning note and nothing else. It goes there rather than in Security because it answers the same question `registration_mode` answers — how an account comes into existence here — and it sits outside that `fieldset` so a registration save does not grey it out. Three stacked hint lines under one label (what it does, that it is not the registration setting, and on an instance with no provider, that nothing is configured for it to govern) is one more than any drawn hint stack. **The artboard has to answer:** whether a panel headed by a radio group can also hold a switch, or whether the two settings want a divider, a second legend, or a panel of their own; how a toggle aligns to a fieldset's controls, which are indented by their radio and this is not; and and whether the not-configured caveat is a third hint line (built that way so `aria-labelledby` reads it out with the switch) or the info-tone `hm-admin-note` the invites screen uses, which would put it outside the switch's accessible name | — (needs the account-creation panel redrawn on `admin-org-settings`, `Hamlaneh Admin.dc.html`) |
| Encrypted-conversation indicator | — | `awaiting-design` — no artboard draws encryption anywhere, and a conversation the server cannot read has to say so on the screen or the promise is only in the docs. Built as plain semantic HTML in `webapp/src/components/chat/E2eeNotice.tsx`: one `<section>` above the message column carrying a sentence, with no glyph, no badge and no colour, because a padlock is a visual decision and inventing one here would be exactly what the pipeline forbids. It also refuses every word engineering principle 4 bars — it states what happens (encrypted on your device, ciphertext on the server) and claims nothing about what that defeats. **The artboard has to answer:** whether this is a persistent strip, a mark in the channel header beside the name, or something in the composer; whether the plaintext case gets a counterpart or stays silent; and whether the three consequences the contract names — no server-side search, no link previews, no mention badges — belong on this surface or in the channel menu | — (needs an encrypted-channel state on `Hamlaneh Chat.dc.html`) |
| Message that cannot be decrypted | — | `awaiting-design` — an ordinary MLS condition, not an error: a message sealed before this device joined the group can never be opened by it, and a device restored from a lost keystore is in the same position. It must not render as an empty bubble and must not crash the list, so `MessageBubble` draws a plain `<span class="hm-msg__undecryptable">` in the bubble's body slot with a sentence saying so — structurally the same slot the delivered "message removed" treatment uses, with **no** styling borrowed from it, since the two say different things and only the designer can say whether they look alike. A short-lived `hm-msg__decrypting` sits in the same slot while the decryption is in flight. **The artboard has to answer:** whether this is the removed-message dashed treatment, its own tone, or a bubble with an inline explanation; whether the reason is worth naming per case (before you joined / this device lost its keys); and whether a decrypting placeholder is drawn at all, or the bubble simply appears once opened | — (needs an undecryptable message state on `Hamlaneh Chat.dc.html`) |
| "Cannot add yet — no key packages" notice | — | `awaiting-design` — a member with no MLS device, or whose key-package pool is empty, cannot be added to the group and therefore cannot read the conversation while everyone else can. The contract answers a claim for them with both lists empty and says in as many words that the client should render "cannot add yet" rather than pretending; this is that. Plain `<p role="status">` inside the same `E2eeNotice` section, naming the people. **The artboard has to answer:** whether this belongs beside the encryption indicator or in the member list where the affected people are; whether it is a warning tone or neutral (nothing is broken — someone has not signed in yet); and what it says when the list is long enough that naming everyone is not an option | — (needs a state on `Hamlaneh Chat.dc.html`) |
| Encryption unavailable in this browser | — | `awaiting-design` — a browser with no IndexedDB, no WebCrypto, or a wasm module that will not load has no honest way to hold MLS state, so encrypted conversations are unreadable and unwritable for that session. It is a state, never a crash and never a silent fall back to plaintext: `E2eeNotice` draws a `<p role="alert">` and the composer is disabled with its own reason line. **The artboard has to answer:** whether this is the connection banner's treatment (it is the same shape of problem — the app works, one capability does not), a bubble-level notice, or a full-column empty state; and whether it offers an action at all, given that there is nothing the reader can do but use another browser | — (needs a state on `Hamlaneh Chat.dc.html`) |
| Encryption statement on create-channel | — | `awaiting-design` — **replaces the encrypt-this-channel checkbox, which is gone.** ADR 011 makes the organization's mode decide what a channel is born as, and the server refuses a request that disagrees with it, so offering a choice would be offering a refusal. `CreateChannelDialog` now states the outcome in one sentence (encrypted, or server-readable and why) plus, under Strict, the note listing what an encrypted channel gives up. It still *sends* the value it displayed rather than omitting it, so a client whose view of the mode is stale is refused by name and says so, instead of silently creating the opposite of what the screen promised about a property fixed forever at creation. **The artboard has to answer:** whether a statement nobody can act on belongs in the dialog at all or in the channel afterwards; whether it reads as reassurance, as a constraint, or as neither; how it is distinguished from the fields above it now that it is not a control; and where the stale-mode refusal appears, given that it is the one failure the person can act on and it arrives after they pressed Create | — (needs the create-channel artboard to carry it, `Hamlaneh Chat.dc.html`) |
| Organization encryption mode (Admin → Org settings) | BRIEFS.md §3 addendum 3b | `awaiting-design` — nothing in the admin set draws an encryption mode; ADR 011 makes it the one org setting that must *not* save as you type, so it sits outside every `hm-admin-panel` on `admin-org-settings` and borrows none of the panel, switch-row or toggle treatment. Built as plain semantic HTML in `webapp/src/components/admin/EncryptionModeSection.tsx`: a heading, the current mode in a sentence, the standing `conversations_outside_mode` count, and a two-radio `fieldset` whose Compliance option is **shown and disabled** with the blunt reason beside it (`aria-describedby`), because hiding it teaches nobody what the product will offer and offering it would be the dishonest toggle. The count is stated at zero too — a number that appears only when non-zero teaches an admin that silence means the mode covers everything. **The artboard has to answer:** whether an option that is deliberately unavailable is a disabled radio, a radio with a badge, or a described-but-unselectable card, and where its three-missing-pieces reason sits without reading as an error; whether a permanent "N conversations are outside this mode" line is a hint, an info note, or a figure worth its own treatment, and what it looks like at zero; and whether a setting with a ceremony belongs on the same screen as a dozen that save on blur at all, or wants a panel, a divider, or a page of its own | — (needs an encryption-mode block on `admin-org-settings`, `Hamlaneh Admin.dc.html`) |
| Encryption-mode switch dialog | BRIEFS.md §3 addendum 3b | `awaiting-design` — the confirmation ADR 011 decision 2 fixes, in both directions. Deliberately **not** the delivered `ConfirmDialog`: that draws a title and *one* sentence, and this disclosure is three or four, each load-bearing — nothing already stored changes, the new mode begins with what is created after it, and (choosing Compliance) a complete export of the past is impossible, which is the product working rather than a defect. Built as a plain `<div role="dialog">` with a heading, those paragraphs, the live count and two bare buttons; `useFocusTrap` is reused because a modal that leaks Tab is broken in any design. The Strict → Compliance direction is unreachable while Compliance is locked, and its copy is unit-tested anyway. **The artboard has to answer:** whether a confirm dialog can carry four paragraphs or whether this is a full screen, a two-step flow, or a typed confirmation; how "this is impossible and that is the point" is drawn so it reads as the guarantee rather than a failure or a warning; whether the two directions share one treatment when only one is reachable; and whether the live count belongs in the dialog, above the confirm button, or beside the mode choice that raised it | — (needs a switch-confirmation state on `Hamlaneh Admin.dc.html`) |
| Encryption statement on the new-direct-message picker | — | `awaiting-design` — **replaces the encrypt-this-conversation checkbox, which is gone**, and answers this row's own last question: the design pipeline asked whether an encrypted DM should be "an org-level default with no per-conversation choice", and ADR 011 decided exactly that. `PeoplePicker` now carries two sentences above the list, present only for the DM picker and not the invite picker (inviting joins a conversation that exists and has already decided): what a new conversation started here will be, and that reopening one that already exists keeps whatever it was opened as whatever the mode is now. Picking the person is still the creation moment, so the second sentence is load-bearing — a reader has no way to tell from the list which people they already have a conversation with. **The artboard has to answer:** whether a picker whose rows are the action can carry two paragraphs above them at all, or whether the statement belongs on the rows, in the entry point, or nowhere; whether the reopening caveat is worth its own line when it applies to some rows and not others; and whether the person rows should distinguish an existing conversation from a new one so the caveat becomes unnecessary | — (needs a DM-picker state on `Hamlaneh Chat.dc.html`) |
| Backup enrolment offer | — | `PENDING` — unstyled plumbing built in slice 3.5 (`plumbing/BackupPlumbing.tsx`, `chat.e2ee.backup.offer*`): four sentences and three actions, drawn at first MLS use on the account and never again on its own. It is an **offer, not a default** — a backup sealed under a key the person never saw is unrestorable by construction (ADR 010, decision 4) — and a decline is recorded and respected, re-surfacing only as the quiet `BackupIndicator` line. The sentence that must not be lost in a redraw is `offerNotMessages`: this saves your trust decisions and **not** your messages, and no artboard may let the reader come away believing otherwise. **The artboard must answer:** whether an offer that arrives unprompted in the middle of a conversation is a strip above the message column, a dialog, or a card in the sidebar; how a screen with four load-bearing sentences and three actions ranks them without making “Not now” the path of least resistance; and where the quiet indicator lives once there is a settings surface for it | — |
| Recovery-key show-once ceremony | — | `PENDING` — unstyled plumbing built in slice 3.5 (`chat.e2ee.backup.ceremony*`): the recovery key as 56 Crockford base32 characters in fourteen groups of four, direction-pinned LTR in both locales because it is typed back character for character on another device. **Shown exactly once and nowhere else** — not stored, not re-openable, not resettable — so the design may not add a “show it again” affordance, and a copy button is the only mechanism that could stand in for one. **The artboard must answer:** how a 56-character string is set so it can be transcribed by hand without losing your place; whether the groups wrap or scroll at 375px; whether there is a copy control at all, given that a clipboard is a copy and the ADR’s claim is that no copy exists; and what makes “this will not be shown again” read as a fact to act on rather than as a warning label to skip | — |
| Backup restore screen | — | `PENDING` — unstyled plumbing built in slice 3.5 (`chat.e2ee.backup.restore*`): key entry, then a **sealed-date confirmation** before anything lands, then the seven refusal sentences. The date is the load-bearing part: on a first restore the device that would hold the anti-rollback floor is the one that was lost, so the human is the only remaining channel to freshness (ADR 010, decision 3) and the screen puts the date to them as a fact to confirm rather than as decoration. The missing-backup answer (`failure.noBackup`) states the server’s answer plainly and treats a missing expected backup as worth suspicion, because deletion and never-backed-up are indistinguishable on the wire. The field is `type="text"` with `autocomplete="off"` and must stay so — a password manager offering to save this would be storing the one thing the design says is stored nowhere. **The artboard must answer:** how a date somebody is being asked to vouch for is set apart from a date that is merely displayed; whether the two steps are one screen or two; and whether a refusal replaces the form or sits under it, given that four of the seven refusals are worth acting on and three are worth re-reading | — |
| No-recovery-key failure path | — | `PENDING` — unstyled plumbing built in slice 3.5 (`chat.e2ee.backup.noKey*`): four sentences, near enough word for word from ADR 010 decision 2. It states that the backup cannot be opened, that nobody — not Hamlaneh, not the administrator — has a copy, that the account and the conversations continue and only the recorded trust decisions are gone, and that this is how the feature is built rather than a fault. **No escalation path may appear on it**: no support link, no “ask your administrator to restore it”, no retry that implies a different answer next time, and a unit test asserts the absence. **The artboard must answer:** whether a dead end is drawn as an error at all, given that nothing is broken; how “bounded loss” is made visible so the reader sees what they keep before what they lost; and whether the honest statement that support cannot help belongs on this screen or would read as pre-emptive defensiveness | — |
| Org logo (Admin → Org settings, shown on login + sidebar) | BRIEFS.md §3 addendum 3a | `PENDING` — **nothing is built**, which makes it the only row here with no plumbing behind it, and it is listed because for a year two documents claimed the opposite: ROADMAP 1.4 ticked "org name, logo, default locale" as delivered and OVERVIEW said the org is customized with a logo. Neither was true — no column, no `OrgSettings` field, no upload endpoint, no `<img>` on either screen — and both have been corrected rather than left to be discovered by whoever implemented the row. BRIEFS §1 asked the designer to "leave a slot where an org logo could appear above/near the wordmark"; the delivered auth set draws the product name as text and no slot came back (see **Brand assets** below), so the slot has to be drawn before the feature can be built to it. **Two facts the artboard cannot be drawn without:** the login page is unauthenticated, so its logo is a public asset served before any session exists — it cannot ride the signed, hour-lived, cookie-less files origin every other upload uses (ADR 003), and whatever serves it is reachable by anyone who can reach the sign-in page; and the upload lives on the admin origin (ADR 015) while both surfaces that display it do not, so the URL has to be absolute-or-rooted rather than relative to wherever it was set. **The artboard has to answer:** what the login screen is when the logo is absent, which is every install on day one and the delivered artboard's own state — whether the wordmark simply stands alone or the slot reserves height either way; what the sidebar shows, given the org *name* is already the first thing in it and a logo beside it is either identity or duplication; what a wrong-shaped upload does (a wide lockup and a square mark cannot both sit in one slot, and the honest answers are a fixed aspect box, a cropper, or a refusal); whether dark theme needs a second asset, since a dark-ink logo on the dark canvas is invisible and nothing else in the product has ever needed two files; and what the admin control looks like between choosing a file and it being live — the settings panel saves on blur and an upload does not | — (needs an org-logo slot on `Hamlaneh Auth.dc.html` and `Hamlaneh Chat.dc.html`, and the control on `admin-org-settings`, `Hamlaneh Admin.dc.html`) |
| Desktop connect screen (Tauri shell) | — | `awaiting-design` — the desktop app's one local screen, and nothing in the four delivered sets covers it: every drawn surface belongs to the web application, which the shell loads from the instance over the network rather than bundling (`desktop/README.md` explains why that is forced). Built as plain semantic HTML in `desktop/ui/index.html` — a heading, one labelled `type="url"` field, a hint, a `role="alert"` paragraph and a submit button — with no stylesheet at all and not one class borrowed from `webapp/`, because the shell has no bundler and giving it one to serve four elements would be inventing a build step to inherit a treatment nobody drew for this screen. Its strings are keyed en/fa in `desktop/ui/connect.js` and their parity is asserted by `desktop/connect.test.js`, since `npm run i18n:check` cannot see them. **The artboard has to answer:** whether the first thing a desktop user meets is a bare form or a front door, given this screen precedes any instance and so can carry no organisation identity — the auth set's centred card on a tinted ground is the nearest delivered treatment and it assumes an instance is already known; what a wrong or unreachable address looks like, which today is the system webview's own error page with no way back short of restarting the app; and whether a remembered instance should connect on launch rather than prefill the field, which is the same question asked as behaviour | — (needs a `desktop-connect` artboard) |

## Brand assets

The symbol SVGs live in `webapp/public/brand/` (the 4096px PNG renders and the kit zip are
deliberately not committed — derived artefacts, and git history is not rewritable).

**RESOLVED 2026-08-21.** The symbol was recoloured to the product palette and the delivered
UI design is unchanged — the two decisions are separate and both hold:

- The mark now uses the exact Quiet Nest tokens: light gradient
  `#123F39 → #194941 → #235C55`, dark gradient `#6FB5AA → #81C9BD → #9ADACF`, tiles on
  `#F7F6F2` and `#111615 → #18201E` (the dark tile is the app's own canvas, no longer navy).
  Flat single-colour variants ship in `flat/` for 16–32px and favicon use, where a three-stop
  gradient collapses into a smudge.
- **No screen changes.** The delivered artboards draw the product name as text and have no slot
  for a symbol, so the mark is not inserted into them. It is a project-level asset: repository
  and product identity, and the fallback wherever a logo is needed but none exists.

All eight SVGs verified self-contained before shipping — no raster, no external references, no
scripts or filters, `role="img"` and `aria-labelledby` intact — which matters because they are
served by an app under a strict CSP.

Still missing: the full lockup (symbol + wordmark) exists only as PNG. An SVG would be needed for
the README and any future site.
